# PlatformIO build artifacts
.pio/

# ESP-IDF build artifacts (from idf.py builds)
build/
sdkconfig
sdkconfig.old
sdkconfig.esp32*
managed_components/

# ESP-IDF component-manager lock. Regenerated per target (the `target:` field
# rewrites itself on every env switch), and the only dependency it pins is IDF
# itself — which platformio.ini already pins harder via platform_packages.
dependencies.lock

# VS Code
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
